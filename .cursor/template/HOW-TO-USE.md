# 📝 ШАБЛОНЫ ДЛЯ НОВЫХ ИНСТРУКЦИЙ

> **Эта папка содержит шаблоны для создания новых инструкций**

---

## 📋 СОДЕРЖИМОЕ

### Шаблоны (.mdc)

| Шаблон | Назначение | Куда положить результат |
|--------|------------|------------------------|
| `_template-protocol.mdc` | Новый протокол для типа задачи | `rules/protocol-*.mdc` |
| `_template-base-module.mdc` | Новый атомарный модуль | `rules/_base-*.mdc` |
| `_template-standard.mdc` | Новый стандарт качества | `rules/standard-*.mdc` |
| `_template-alone.mdc` | Новая одиночная инструкция | `rules_alone/*.mdc` |

### Reference материалы

| Файл | Назначение |
|------|------------|
| `AGENT job description writer.md` | Job Description формат для AI агентов — Input/Output/Guardrails/WHY |
| `snt_ajtdb_landing_evaluation v3.md` | AJTBD методология (Ваня Замесин) — полный анализ лендингов/интерфейсов |
| `agents.md-main.zip` | Стандарт AGENTS.md — quick start для AI агентов |

---

## 🔧 КАК ИСПОЛЬЗОВАТЬ

### Шаг 1: Выбери тип инструкции

| Если нужно... | Используй шаблон |
|---------------|------------------|
| Новый workflow для типа задачи | `_template-protocol.mdc` |
| Переиспользуемое правило | `_template-base-module.mdc` |
| Стандарт проверки качества | `_template-standard.mdc` |
| Инструкция для единичных задач | `_template-alone.mdc` |

### Шаг 2: Скопируй шаблон
```bash
cp .cursor/template/_template-protocol.mdc .cursor/rules/protocol-myprotocol.mdc
```

### Шаг 3: Заполни шаблон

**Обязательные элементы:**
- `description:` — краткое описание назначения
- Секция WHY — почему это правило важно
- Секция INPUT — что инструкция получает
- Секция OUTPUT — что должна произвести
- Секция GUARDRAILS — ограничения и границы

> **Reference:** `AGENT job description writer.md` содержит полный формат Job Description

### Шаг 4: Интегрируй
- Для `protocol-*`: Добавь в таблицу `core-master.mdc`
- Для `_base-*`: Добавь ссылки в протоколы
- Для `standard-*`: Добавь в VERIFY-PHASE протоколов
- Для `rules_alone/`: Обнови `rules_alone/HOW-TO-USE.md`

### Шаг 5: Обнови документацию
- `rules/ARCHITECTURE.md` — добавь в структуру
- `.cursor/CONTEXT.md` — запиши изменение в CHANGELOG
- `.cursor/HOW-TO-USE.md` — обнови если нужно

---

## 📖 NAMING CONVENTIONS

### Протоколы
```
protocol-{action}.mdc
```
Примеры: `protocol-development`, `protocol-research`, `protocol-bugfix`, `protocol-prepare-prompt`

### Базовые модули
```
_base-{concept}.mdc
```
Примеры: `_base-confidence`, `_base-challenge`, `_base-5wh`, `_base-jtbd-thinking`

### Стандарты
```
standard-{area}.mdc
```
Примеры: `standard-qa`, `standard-tdd`, `standard-rca`, `standard-agent-quality`

### Одиночные
```
{descriptive-name}.mdc
```
Примеры: `core-duplicate-check`, `from-the-end`, `backlog-to-rules`

---

## 📋 ФОРМАТ СЕКЦИЙ (Job Description стиль)

При создании новой инструкции используй этот формат для каждой значимой секции:

```markdown
## [НАЗВАНИЕ СЕКЦИИ]

**Input:**
- Что инструкция/проверка получает на вход

**Output:**
- Что должна произвести

**Guardrails:**
- ❌ Что НЕ делать
- ✅ Что обязательно сделать

**WHY:**
> Объяснение почему это правило важно.
> Пример ошибки, которую оно предотвращает.
```

**Пример:**
```markdown
## 3.5. Type Consistency Check (API Integration)

**Input:**
- Данные из PostgreSQL/Bitrix24 API
- Map/Set структуры с числовыми ключами

**Output:**
- Правильно типизированные данные
- Работающие Map.get() / Set.has()

**Guardrails:**
- ❌ Не использовать BigInt напрямую без конвертации
- ✅ Всегда приводить ID к Number() перед использованием в Map/Set

**WHY:**
> PostgreSQL bigint → JavaScript string при JSON передаче.
> Map.get("123") не найдёт значение по ключу 123 (number).
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **НИКОГДА** не добавляй `alwaysApply: true` (кроме core-master.mdc)
2. **ВСЕГДА** заполняй `description:` в шапке файла
3. **ССЫЛАЙСЯ** на `_base-*` модули вместо дублирования
4. **НЕ ДУБЛИРУЙ** — если правило уже есть, используй его
5. **ДОБАВЛЯЙ WHY** — объясняй почему правило важно
6. **ПРОВЕРЯЙ quality** — используй `standard-agent-quality.mdc` как референс

---

## 🔄 WORKFLOW СОЗДАНИЯ НОВОЙ ИНСТРУКЦИИ

```
1. Идея → 2. Выбор типа → 3. Копирование шаблона
    ↓
4. Заполнение (Input/Output/Guardrails/WHY) → 5. Тестирование → 6. Интеграция
    ↓
7. Документирование → 8. Коммит
```

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

| Файл | Назначение |
|------|------------|
| `rules/ARCHITECTURE.md` | Структура всех инструкций |
| `rules/standard-agent-quality.mdc` | Quality framework (метрики, границы) |
| `.cursor/CONTEXT.md` | История изменений |
| `rules_alone/backlog-to-rules.mdc` | Процесс внедрения улучшений |

---

**Дата создания:** 2025-12-26
**Обновлено:** 2026-01-09
**Версия:** 2.0

