---
description: "Применять при Development для Test-Driven Development подхода. Workflow: тест-кейсы ПЕРЕД реализацией, минимальный код для прохождения, рефакторинг после зелёных тестов."
globs: 
alwaysApply: false
tags: [tdd, testing, development]
---

# 🧪 STANDARD: TDD

## Context

- **Когда:** Development, особенно для логики и API
- **Принцип:** Red → Green → Refactor
- **Связь:** `protocol-development.mdc`

---

## Requirements

### TDD Workflow

<required>
  1. **Write Test** — тест должен ПАДАТЬ (red)
  2. **Write Code** — минимальный код для прохождения
  3. **Run Test** — должен пройти (green)
  4. **Refactor** — улучшить код, тесты зелёные
</required>

### Test Cases Template

```markdown
## 📋 TEST CASES (BEFORE implementation)

| # | Input | Expected Output | Status |
|---|-------|-----------------|--------|
| 1 | [input] | [expected] | ⏳ |
| 2 | [edge case] | [expected] | ⏳ |
| 3 | [error case] | [error msg] | ⏳ |
```

### Coverage Requirements

| Type | What to test |
|------|--------------|
| Happy path | Normal flow |
| Edge cases | Empty, null, boundary |
| Error cases | Invalid input, exceptions |

---

## Examples

<example>
GOOD: TDD flow
```markdown
## Test Cases:
| # | Input | Expected | Status |
|---|-------|----------|--------|
| 1 | [1,2,3] | 6 | ⏳ |
| 2 | [] | 0 | ⏳ |
| 3 | null | Error | ⏳ |

// Step 1: Write test (red)
test('sum of [1,2,3] = 6', () => {
  expect(sum([1,2,3])).toBe(6);
});
// FAIL ❌

// Step 2: Write code (green)
function sum(arr) { return arr.reduce((a,b) => a+b, 0); }
// PASS ✅

// Step 3: Next test...
```
</example>

---

## Critical Points

<critical>
  - Тесты ПЕРЕД кодом
  - Тест должен сначала ПАДАТЬ
  - Минимальный код для прохождения
  - Refactor только после зелёных тестов
</critical>

---

**Версия:** 1.1
