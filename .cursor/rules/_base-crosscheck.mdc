---
description: "ВСЕГДА применять независимую проверку результатов перед завершением. Требует открыть и прочитать каждый созданный/изменённый файл, проверить результат ДРУГИМ методом. Без Cross-check уверенность -30%."
globs: 
alwaysApply: true
---

# 🔍 CROSS-CHECK — Независимая проверка

## Context

- **Когда:** Перед КАЖДЫМ "готово"
- **Роль:** Независимая верификация результата
- **Результат:** Таблица проверенных артефактов

<critical>
  НЕ говори "готово" без независимой проверки!
</critical>

---

## Requirements

<required>
  - Каждый созданный/изменённый файл ОТКРЫТЬ и ПРОЧИТАТЬ
  - Проверить результат ДРУГИМ методом (не тем которым делал)
  - Заполнить таблицу артефактов
  - Сравнить результат с буквальным запросом пользователя
</required>

### Методы Cross-check:

| Сделал | Проверка |
|--------|----------|
| Создал файл | Открой и прочитай |
| Изменил код | Запусти linter |
| API endpoint | curl + код |
| Данные | pandas + SQL |
| UI | Визуальная проверка |

### Формат отчёта:

```markdown
| Файл | Открыт | Проверен |
|------|--------|----------|
| [путь] | ✅/❌ | ✅/❌ |
```

---

## Examples

<example>
GOOD: Cross-check с таблицей
```markdown
| Файл | Открыт | Проверен |
|------|--------|----------|
| src/utils/helper.ts | ✅ | ✅ — linter 0 errors |
| src/api/routes.ts | ✅ | ✅ — curl 200 OK |
```
</example>

<example type="invalid">
BAD: Без cross-check
```
Файлы созданы успешно. Готово!
```
Проблема: Файлы не открывались, не проверялись. Уверенность -30%.
</example>

---

## Critical Points

<critical>
  - Если Cross-check не выполнен → Уверенность -30%
  - Два метода — если проверял одним, проверь другим
  - Gaps — если нашёл расхождение, ИСПРАВЬ перед "готово"
</critical>

---

**Версия:** 2.1
