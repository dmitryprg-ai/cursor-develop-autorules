---
description: Атомарный модуль cross-check — независимая проверка результатов
alwaysApply: false
---
# 🔍 CROSS-CHECK PROTOCOL

## COMPACT VERSION (для inline использования)

```
Cross-check = проверка ДРУГИМ методом:
• Файл создан → Открой и прочитай
• API работает → curl + код
• Данные верны → pandas + SQL

НЕ говори "готово" без независимой проверки!
```

---

## Принцип

**Не подтверждай что что-то сделано без НЕЗАВИСИМОЙ проверки.**

Cross-check = проверка результата ДРУГИМ методом, чем тем, которым его получил.

## Методы Cross-Check

### 1. Проверка артефактов
```markdown
## 📁 ARTIFACT CROSS-CHECK

| Артефакт | Открыт | Проверен | Статус |
|----------|--------|----------|--------|
| [путь к файлу 1] | ✅/❌ | ✅/❌ | OK/ISSUE |
| [путь к файлу 2] | ✅/❌ | ✅/❌ | OK/ISSUE |
```

**Правило:** Каждый созданный/изменённый файл должен быть ОТКРЫТ и ПРОЧИТАН.

### 2. Проверка данных
```python
# Метод 1: Через pandas
df.shape, df.head(), df.dtypes

# Метод 2: Через DuckDB (независимая проверка)
duckdb.query("SELECT COUNT(*), COUNT(DISTINCT id) FROM 'file.parquet'")

# Метод 3: Через shell
wc -l file.csv
head -5 file.csv
```

### 3. Проверка API
```markdown
## 🔗 API CROSS-CHECK

| Endpoint | Метод 1 (curl) | Метод 2 (код) | Совпадают |
|----------|----------------|---------------|-----------|
| /api/deals | 200 OK, 15 items | 200 OK, 15 items | ✅ |
```

### 4. Проверка кода
```markdown
## 💻 CODE CROSS-CHECK

| Проверка | Статус |
|----------|--------|
| Linter errors = 0 | ✅/❌ |
| TypeScript errors = 0 | ✅/❌ |
| Tests pass | ✅/❌ |
| Console errors = 0 | ✅/❌ |
```

### 5. Проверка UI изменений

**После любых UI изменений:**

```markdown
## 🖥️ UI CROSS-CHECK

### Уточнение требований:
- [ ] Нужен responsive layout или фиксированный?
- [ ] На каких breakpoints проверить?
- [ ] Есть ли дизайн-макет для сравнения?

### Проверка:
| Breakpoint | Ожидание | Фактически | OK? |
|------------|----------|------------|-----|
| Desktop (>1024px) | [описание] | [что видно] | ✅/❌ |
| Tablet (768-1024px) | [описание] | [что видно] | ✅/❌ |
| Mobile (<768px) | [описание] | [что видно] | ✅/❌ |

### Указание пользователю:
"Пожалуйста, проверьте визуально на [URL]. Особенно обратите внимание на [что проверить]."
```

**Типичные ошибки:**
- `grid-cols-1 md:grid-cols-3` — на mobile будет 1 колонка (возможно не то что нужно)
- Успешная сборка ≠ корректное отображение
- Responsive по умолчанию — не всегда правильный выбор

**Правило:** Уточняй responsive требования ПЕРЕД реализацией, не предполагай.

> **WHY:** Применил responsive grid вместо фиксированного — пользователю пришлось указывать исправление.

### 6. Cross-check: Output vs Literal Request (из Improvement #13)

**Input:** Результат работы + оригинальный запрос пользователя

**Output:** Подтверждение буквального соответствия

**Процесс:**

```markdown
## 📋 OUTPUT vs REQUEST CROSS-CHECK

### Оригинальный запрос (цитата):
> "[Скопировать БУКВАЛЬНО запрос пользователя]"

### Explicit Constraints из запроса:
- НЕ делать: "[цитата]"
- СДЕЛАТЬ: "[цитата]"
- КАК делать: "[цитата]"

### Проверка соответствия:
| Требование (цитата) | Выполнено буквально? | Что сделано |
|---------------------|----------------------|-------------|
| "[требование 1]" | ✅/❌ | [что именно] |
| "[требование 2]" | ✅/❌ | [что именно] |

### Red Flags:
- [ ] Добавлено что-то "от себя" чего не просили?
- [ ] Сделано "как лучше" вместо "как просили"?
- [ ] Есть расхождение между запросом и результатом?
```

**Guardrails:**
- ❌ НЕ объявлять "готово" если есть расхождения
- ❌ НЕ интерпретировать — сравнивать БУКВАЛЬНО
- ✅ Если есть Red Flag — СТОП и исправить

> **WHY:** AI скопировал .cursor внутрь git/, хотя пользователь сказал "скопирую 2 папки сам". 
> Cross-check не включал сравнение с буквальным текстом запроса. Потеря ~30 минут.

---

## Gap Analysis Template

```markdown
## 🔍 GAP ANALYSIS: Expected vs Actual

### Expected Output:
[Что ДОЛЖНО было быть]

### Actual Output:
[Что ПОЛУЧИЛОСЬ фактически]

### Gaps Found:
| # | Expected | Actual | Gap | Action |
|---|----------|--------|-----|--------|
| 1 | [ожидали] | [получили] | [разница] | [как исправить] |

### Root Cause of Gaps:
- [Почему gap 1 существует]
- [Почему gap 2 существует]
```

## Checklist Cross-Check

```markdown
## ✅ CROSS-CHECK CHECKLIST

### Артефакты:
- [ ] Каждый файл открыт и прочитан
- [ ] Содержимое соответствует ожиданиям
- [ ] Нет мусорных/временных файлов

### Данные:
- [ ] Проверены минимум 2 независимыми методами
- [ ] Количество записей совпадает
- [ ] Структура данных корректна

### Код:
- [ ] 0 linter errors
- [ ] 0 TypeScript errors
- [ ] Тесты проходят
- [ ] Нет console errors

### JTBD (для user-facing фич):
- [ ] Job Story реализована (Когда..., хочу..., чтобы...)
- [ ] Выгоды (🟨) ≥60%, Налоги (⬜) ≤40%
- [ ] UI тексты про результат, не про средства
- [ ] Минимум шагов до достижения цели

### Результат:
- [ ] Expected = Actual
- [ ] Gaps задокументированы
- [ ] Risks identified
```

## Правила

1. **НЕ ОБЪЯВЛЯЙ "готово"** пока cross-check не выполнен
2. **ИСПОЛЬЗУЙ РАЗНЫЕ МЕТОДЫ** — если проверял pandas, проверь ещё DuckDB/SQL
3. **ДОКУМЕНТИРУЙ GAPS** — даже маленькие расхождения важны
4. **ЧЕСТНО ЗАПИСЫВАЙ** — если cross-check показал проблемы, не скрывай

---

**Версия:** 1.1
**Обновлено:** 2026-01-10 (добавлена секция 6 - Output vs Literal Request)
**Использование:** Вызывается из `core-master.mdc` в фазе VERIFY
