---
description:
globs:
alwaysApply: true
tags: [rules, cursor, standard]
---

# 🧩 Cursor rules: standard format (always)

## Context

- Применять каждый раз, когда создаю/обновляю файлы правил в `.cursor/rules/*` или `.cursor/rules_alone/*`
- Стандарт описан в `.cursor/rules/standart-generating-agent.mdc`

## Requirements

- Всегда проверять, нет ли уже существующего rule-файла для нужной темы (не создавать дубль)
- Всегда соблюдать нейминг: `.cursor/rules/{CATEGORY}-rule-name-{auto|agent|manual|always}.mdc`
- Всегда использовать kebab-case в имени файла
- Для **always** правил:
  - `alwaysApply: true`
  - `description:` пусто
  - `globs:` пусто
  - имя файла заканчивается на `-always.mdc`
- Всегда включать секции: `Context`, `Requirements`, `Examples` (valid + invalid), `Critical Points`
- **УНИВЕРСАЛЬНОСТЬ:** правила должны работать в любом проекте:
  - НЕ включать конкретные названия сущностей проекта (`DealsTable`, `BitrixService`)
  - НЕ включать проектную архитектуру (`module.types.ts → ...`)
  - НЕ включать проектные интеграции (`Bitrix24`, `PostgreSQL bigint`)
  - Использовать плейсхолдеры (`<ComponentName>`, `<ServiceName>`, `<url>`)
  - Проектную специфику выносить в `AGENTS.md`
- После создания/обновления rule-файла всегда перечитывать его (self-check)
- После создания/обновления rule-файла в ответе всегда выводить:
  - Rule {Created or Updated}: path
  - Rule Type
  - Rule Description: точное значение поля `description`

## Examples

<example>
Создал always-правило:
- имя файла: workflows-site-basic-auth-always.mdc
- description/globs пустые
- alwaysApply: true
- есть valid+invalid examples
</example>

<example type="invalid">
Неверно:
- имя файла без суффикса -always.mdc
- нет секций Examples или нет invalid example
- description заполнено у always-правила
- правило содержит проектную специфику (`DealsTable`, `Bitrix24`, `module.types.ts`)
</example>

## Critical Points

<critical>
  - ALWAYS: следовать `.cursor/rules/standart-generating-agent.mdc` при создании/изменении правил
  - NEVER: создавать дубли правил (сначала поиск/обновление существующего)
  - ENSURE: структура и нейминг соответствуют стандарту
</critical>

