---
description: Атомарный модуль использования todo_tool для декомпозиции задач
alwaysApply: false
---
# 📋 ИСПОЛЬЗОВАНИЕ TODO_TOOL

## Когда использовать

**ИСПОЛЬЗУЙ todo_tool если:**
- Задача требует более 2-3 шагов
- Есть зависимости между шагами
- Нужно отслеживать прогресс
- Задача сложная или многокомпонентная

**НЕ используй todo_tool для:**
- Простых одношаговых задач
- Ответов на вопросы
- Чтения файлов

## Правила создания TODO

### 1. Декомпозиция
```
Разбей большую задачу на атомарные шаги (1 TODO = 1 действие)
```

### 2. Статусы
| Статус | Когда использовать |
|--------|-------------------|
| `pending` | Ещё не начат |
| `in_progress` | Сейчас работаю (только 1 одновременно!) |
| `completed` | Выполнен и проверен |
| `cancelled` | Отменён (изменились требования) |

### 3. Правило одного in_progress
```
ТОЛЬКО ОДИН TODO может быть in_progress одновременно!
```

### 4. Обновляй по мере работы
```
Завершил шаг → сразу отметь completed → начни следующий
```

## Шаблон TODO

```javascript
{
  "id": "уникальный-id",
  "content": "Конкретное действие (до 70 символов)",
  "status": "pending | in_progress | completed | cancelled"
}
```

## Что НЕ включать в TODO

❌ **НЕ включай:**
- Linting / проверку ошибок (это часть каждого шага)
- Поиск по кодовой базе (это инструмент, не задача)
- Чтение файлов (это подготовка)
- "Проверить" без конкретики

✅ **Включай:**
- Конкретные изменения в коде
- Создание конкретных файлов
- Интеграцию с конкретными компонентами

## Пример хорошего TODO списка

```javascript
[
  {"id": "1", "content": "Создать API endpoint /api/deals для получения сделок", "status": "completed"},
  {"id": "2", "content": "Добавить фильтрацию по дате в /api/deals", "status": "in_progress"},
  {"id": "3", "content": "Создать UI компонент DealsTable с пагинацией", "status": "pending"},
  {"id": "4", "content": "Интегрировать DealsTable с API endpoint", "status": "pending"}
]
```

---
**Использование:** Вызывается из `core-master.mdc` и всех протоколов
