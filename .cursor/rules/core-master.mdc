---
description: Master Router Protocol — единственная точка входа для всех AI инструкций
alwaysApply: true
---
# 🎯 MASTER ROUTER PROTOCOL

> **Это единственный файл с `alwaysApply: true`**
> Все остальные инструкции активируются через него
> **Quick Start:** `AGENTS.md` в корне проекта

---

## 0. ПОДГОТОВКА ПРОМТА (ОБЯЗАТЕЛЬНО для STANDARD/COMPLEX)

**Перед выполнением задачи улучши промт пользователя:**

### Для 🟢 SIMPLE — пропустить, выполнять сразу

### Для 🟡 STANDARD — быстрая подготовка:
```markdown
## 📝 QUICK PROMPT PREP

**Исходный:** [промт пользователя]

**Улучшенный:**
- Goal: [конкретный результат]
- Files: [какие файлы затронуты]
- Constraints: [что НЕ менять]
- Success: [как проверить готовность]
- Protocol: [какой протокол применить]

→ Выполняю по улучшенному промту...
```

### Для 🔴 COMPLEX — полная подготовка:
```
Применить: .cursor/rules/protocol-prepare-prompt.mdc
```

> **WHY:** По данным [cursor.com/docs](https://cursor.com/docs/cookbook/large-codebases): 
> "Spending an above-average amount of thought to create a precise, well-scoped plan 
> can significantly improve output quality."

---

## 1. ОПРЕДЕЛЕНИЕ СЛОЖНОСТИ ЗАДАЧИ

**Определи сложность — от неё зависит глубина применения протоколов:**

| Сложность | Признаки | Что применять |
|-----------|----------|---------------|
| 🟢 **SIMPLE** | Изменение текста/стилей, исправление typo, изменение константы, 1-2 файла | Только VERIFY (linter + visual check), пропустить JTBD/Duplicate Check |
| 🟡 **STANDARD** | Добавление фичи, исправление бага, рефакторинг функции, несколько файлов | Полный протокол по типу задачи + BASE MODULES |
| 🔴 **COMPLEX** | Архитектурные решения, метод с многими потребителями, миграция данных, критические системы | Полный протокол + `standard-cto-review.mdc` + все BASE MODULES |

### Как определить сложность:

```
🟢 SIMPLE если:
   • Изменение локализовано (1-2 файла)
   • Нет зависимостей от других модулей
   • Очевидный результат

🟡 STANDARD если:
   • Новая функциональность
   • Затрагивает логику
   • Несколько связанных изменений

🔴 COMPLEX если:
   • Изменение может сломать другие места
   • Много потребителей у изменяемого кода
   • Требуется архитектурное решение
   • Работа с критическими данными
```

---

## 2. ОПРЕДЕЛЕНИЕ ТИПА ЗАДАЧИ

**Прочитай запрос пользователя и определи тип задачи:**

| Тип задачи | Ключевые слова | Протокол | Дополнительно |
|------------|----------------|----------|---------------|
| **Data Research** | данные, parquet, csv, анализ, исследование, pandas | `protocol-research.mdc` | `_base-5wh.mdc` |
| **Feature Development** | добавить, создать, новая фича, разработать | `protocol-development.mdc` | `standard-tdd.mdc`, `_base-jtbd-thinking.mdc` |
| **Bug Fix** | ошибка, баг, не работает, падает, исправить | `protocol-bugfix.mdc` | `standard-rca.mdc`, `_base-5wh.mdc` |
| **Refactoring** | рефакторинг, улучшить код, оптимизировать | `protocol-refactoring.mdc` | `standard-qa.mdc` |
| **Code Review** | ревью, проверка кода, оценить | `standard-qa.mdc` | `standard-cto-review.mdc` |
| **Freeze Recovery** | завис, freeze, зациклился | `protocol-freeze-recovery.mdc` | `standard-rca.mdc` |
| **Session Review** | review сессии, конец сессии | `protocol-session-review.mdc` | — |

---

## 3. BASE MODULES — ПРИМЕНЯЙ ВСЕГДА (STANDARD/COMPLEX)

### 3.1. 📊 КАЛИБРОВКА УВЕРЕННОСТИ (`_base-confidence.mdc`)

**ОБЯЗАТЕЛЬНО после КАЖДОГО значимого действия:**

```
Уверенность = 100% минус:
• Код не протестирован: -50%
• Артефакты не открыты: -40%
• Нет cross-check: -30%
• Предположения не проверены: -25%

Порог: <80% = не готово, уточнить у пользователя
```

**Формат вывода:**
```
**Уверенность: [до]% → [после]% — [причина]**
```

**Примеры:**
- `Уверенность: 95% → 45% — код написан, но не запускал`
- `Уверенность: 90% → 50% — создал файлы, но не проверил содержимое`
- `Уверенность: 85% → 90% — проверил артефакты, cross-check выполнен`

---

### 3.2. 🎯 CHALLENGE PROTOCOL (`_base-challenge.mdc`)

**ПЕРЕД каждым "готово" — задай себе 4 вопроса:**

```
1. Как опровергнуть мой вывод?
2. Открыл ли я ВСЕ созданные/изменённые файлы?
3. Какие edge cases пропустил?
4. Решает ли это реальный Job пользователя?

Если не можешь ответить → НЕ ГОТОВО
```

**Анти-паттерны (ЗАПРЕЩЕНО):**
- ❌ "Успешно завершено" без проверки
- ❌ "Всё работает" без тестирования
- ❌ "Готово" без открытия артефактов

---

### 3.3. 🔍 CROSS-CHECK (`_base-crosscheck.mdc`)

**Проверка ДРУГИМ методом — НЕ говори "готово" без этого:**

```
Cross-check правила:
• Файл создан → Открой и прочитай
• API работает → curl + код
• Данные верны → pandas + SQL
• UI изменён → Визуальная проверка
```

**Шаблон проверки артефактов:**
```markdown
| Артефакт | Открыт | Проверен | Статус |
|----------|--------|----------|--------|
| [файл 1] | ✅/❌ | ✅/❌ | OK/ISSUE |
```

---

### 3.4. 📋 TODO USAGE (`_base-todo-usage.mdc`)

**Если задача требует >2 шагов — ИСПОЛЬЗУЙ todo_tool:**

```
Когда создавать TODO:
• Задача с 3+ шагами
• Multi-file изменения
• Несколько зависимых действий

Правила:
• Только один "in_progress" одновременно
• Помечай "completed" сразу после выполнения
• Не создавай для простых задач
```

---

### 3.5. ⛔ FORBIDDEN (`_base-forbidden.mdc`)

**Критические запреты — НИКОГДА не нарушай:**

```
❌ НЕ создавать файлы с _fixed, _final, _v2
❌ НЕ объявлять "готово" без проверки артефактов
❌ НЕ игнорировать ошибки в терминале
❌ НЕ делать предположений без проверки данных
❌ НЕ использовать alwaysApply: true в других файлах
```

---

## 4. ПОРЯДОК ВЫПОЛНЕНИЯ (АДАПТИВНЫЙ ПО СЛОЖНОСТИ)

### 🟢 SIMPLE TASK FLOW:
```
EXECUTE → VERIFY (linter) → COMPLETION REPORT
```

### 🟡 STANDARD TASK FLOW:
```
┌─────────────────────────────────────────────────────────────┐
│  1. PLAN PHASE                                              │
│     → Определить тип и протокол                             │
│     → Создать TODO list если >2 шагов                       │
│     → Записать ожидаемый OUTPUT                             │
├─────────────────────────────────────────────────────────────┤
│  2. EXECUTE PHASE                                           │
│     → Применить протокол                                    │
│     → После каждого шага: Уверенность X% → Y%              │
│     → Маленькие шаги с проверкой                            │
├─────────────────────────────────────────────────────────────┤
│  3. VERIFY PHASE                                            │
│     → Cross-check: открыть и проверить артефакты            │
│     → Challenge: 4 вопроса перед "готово"                   │
│     → Linter errors = 0                                     │
│     → standard-agent-quality.mdc (metrics)                  │
├─────────────────────────────────────────────────────────────┤
│  4. COMPLETION REPORT (обязательно!)                        │
│     → Список использованных инструкций                      │
│     → Финальная уверенность                                 │
│     → Auto Learning если были ошибки                        │
└─────────────────────────────────────────────────────────────┘
```

### 🔴 COMPLEX TASK FLOW:
```
STANDARD FLOW + CTO Review + Повышенная верификация + Session Review
```

---

## 5. ЗАПРЕЩЁННЫЕ ДЕЙСТВИЯ (FORBIDDEN)

**Полный список:** `.cursor/rules/_base-forbidden.mdc`

**Критические запреты:**
- ❌ НЕ создавать файлы с постфиксами `_fixed`, `_final`, `_FINAL`, `_new`, `_v2`
- ❌ НЕ объявлять "готово" без проверки артефактов
- ❌ НЕ использовать `alwaysApply: true` в других файлах (только этот!)
- ❌ НЕ дублировать код — использовать `_base-*` модули
- ❌ НЕ игнорировать ошибки в терминале
- ❌ НЕ делать предположений без проверки данных

---

## 6. ОБУЧЕНИЕ НА ОШИБКАХ

**После каждой ошибки или RCA:**
```
Применить: .cursor/rules/error-learning.mdc
```

Записывай все ошибки в `{projectname}-error-log.md` для накопления knowledge base.

---

## 7. БЫСТРЫЙ ВЫБОР ПРОТОКОЛА

### Если не уверен какой протокол применить:

1. **Есть ошибка или проблема?** → `protocol-bugfix.mdc`
2. **Работа с данными (parquet, csv)?** → `protocol-research.mdc`  
3. **Создание нового кода или доработка текущего?** → `protocol-development.mdc`
4. **Улучшение существующего?** → `protocol-refactoring.mdc`
5. **AI завис или зациклился?** → `protocol-freeze-recovery.mdc`

---

## 8. ССЫЛКИ НА ВСЕ ИНСТРУКЦИИ

### Основные протоколы (7):
- `protocol-prepare-prompt.mdc` — подготовка и улучшение промта
- `protocol-development.mdc` — разработка новых фич
- `protocol-research.mdc` — исследование данных
- `protocol-bugfix.mdc` — исправление ошибок
- `protocol-refactoring.mdc` — рефакторинг кода
- `protocol-freeze-recovery.mdc` — восстановление после зависаний
- `protocol-session-review.mdc` — анализ сессии

### Стандарты качества (5):
- `standard-agent-quality.mdc` — метрики успеха и границы агента
- `standard-qa.mdc` — QA стандарты
- `standard-rca.mdc` — Root Cause Analysis
- `standard-tdd.mdc` — Test-Driven Development
- `standard-cto-review.mdc` — CTO/Lead Review

### Базовые модули (7):
- `_base-confidence.mdc` — калибровка уверенности
- `_base-todo-usage.mdc` — использование todo_tool
- `_base-challenge.mdc` — протокол challenge
- `_base-5wh.mdc` — формат 5W+H
- `_base-forbidden.mdc` — запрещённые действия
- `_base-crosscheck.mdc` — cross-check правила
- `_base-jtbd-thinking.mdc` — JTBD-мышление для user-facing фич

### Одиночные инструкции:
- `rules_alone/core-duplicate-check.mdc` — проверка дубликатов
- `rules_alone/from-the-end.mdc` — валидация с конца
- `rules_alone/ajtbd-evaluation.mdc` — полный AJTBD-анализ
- `rules_alone/backlog-to-rules.mdc` — внедрение улучшений

---

## 9. AUTO SESSION LEARNING (в конце каждой задачи)

**Быстрая рефлексия (30 секунд) в конце CLEAN PHASE:**

### 🟢 SIMPLE — minimal learning:
```
Была ошибка? → Если да, одна строка в improvements-backlog
```

### 🟡 STANDARD — brief learning:
```markdown
## Quick Learning: [дата]
- **Задача:** [что делали]
- **Ошибка:** [была/не была] → [что именно, если была]
- **Идея:** [что можно улучшить в инструкциях]
```

### 🔴 COMPLEX — full session review:
```
Применить полный protocol-session-review.mdc
```

**Куда записывать:** `{projectname}-improvements-backlog.md`

---

## 10. ✅ COMPLETION REPORT (ОБЯЗАТЕЛЬНО в конце КАЖДОЙ задачи)

**После завершения работы ОБЯЗАТЕЛЬНО выводи отчёт:**

```markdown
---

## ✅ COMPLETION REPORT

**Сложность:** 🟢 SIMPLE / 🟡 STANDARD / 🔴 COMPLEX

**Использованные инструкции:**
- **Protocol:** [название протокола]
- **Standards:** [список стандартов через запятую]
- **Base Modules:** [список _base-* модулей через запятую]

**Проверки:**
- [ ] Cross-check выполнен
- [ ] Challenge protocol пройден
- [ ] Linter errors = 0

**Финальная уверенность:** X% — [причина]

---
```

### Пример для 🟡 STANDARD задачи:

```markdown
---

## ✅ COMPLETION REPORT

**Сложность:** 🟡 STANDARD

**Использованные инструкции:**
- **Protocol:** protocol-development.mdc
- **Standards:** standard-tdd.mdc, standard-agent-quality.mdc
- **Base Modules:** _base-confidence, _base-challenge, _base-crosscheck, _base-forbidden

**Проверки:**
- [x] Cross-check выполнен (все файлы открыты и проверены)
- [x] Challenge protocol пройден (4 вопроса)
- [x] Linter errors = 0

**Финальная уверенность:** 92% — код проверен, артефакты открыты, cross-check выполнен

---
```

### Пример для 🟢 SIMPLE задачи:

```markdown
---

## ✅ COMPLETION REPORT

**Сложность:** 🟢 SIMPLE
**Использованные инструкции:** core-master.mdc (verify only)
**Linter errors:** 0
**Уверенность:** 95%

---
```

---

## 11. CHECKLIST ПЕРЕД ОТВЕТОМ

```markdown
## ✅ MASTER CHECKLIST

### Сложность определена:
- [ ] 🟢 SIMPLE / 🟡 STANDARD / 🔴 COMPLEX

### Тип задачи определён:
- [ ] Выбран правильный протокол
- [ ] BASE MODULES применены (для STANDARD/COMPLEX)

### План есть (для STANDARD/COMPLEX):
- [ ] Ожидаемый OUTPUT записан
- [ ] TODO list создан (если задача сложная)

### Проверки сделаны:
- [ ] Уверенность откалибрована после каждого шага
- [ ] Cross-check выполнен
- [ ] Challenge protocol применён

### Артефакты проверены:
- [ ] Каждый файл открыт и проверен
- [ ] Linter errors = 0

### COMPLETION REPORT:
- [ ] Отчёт с использованными инструкциями выведен
```

---

**Версия:** 2.0
**Дата:** 2026-01-09

