# 📖 КАК РАБОТАТЬ С CURSOR RULES

## Краткий ответ

**НЕТ, тебе НЕ нужно писать "используй core-master.mdc".**

Файлы с `alwaysApply: true` применяются **АВТОМАТИЧЕСКИ** к каждому запросу.

---

## Как это работает

### Автоматическое применение

```
Твой запрос 
    ↓
[STANDARD/COMPLEX] → protocol-prepare-prompt.mdc (улучшение промта)
    ↓
core-master.mdc (определяет сложность и тип)
    ↓
protocol-*.mdc (выполняет задачу)
    ↓
standard-*.mdc (верификация)
```

Поскольку `core-master.mdc` имеет `alwaysApply: true`, он **всегда** применяется.

---

## Структура папки rules/

```
rules/
├── core-master.mdc           # ⭐ Entry point (alwaysApply: true)
├── _base-*.mdc               # 📦 Базовые модули (7 шт)
├── protocol-*.mdc            # 🔄 Протоколы (7 шт)
├── standard-*.mdc            # 📋 Стандарты (5 шт)
└── error-learning.mdc        # 📚 Обучение на ошибках
```

### Полный список файлов

| Категория | Файлы |
|-----------|-------|
| Entry Point | `core-master` |
| Base Modules (7) | `_base-confidence`, `_base-todo-usage`, `_base-challenge`, `_base-5wh`, `_base-forbidden`, `_base-crosscheck`, `_base-jtbd-thinking` |
| Protocols (7) | `protocol-prepare-prompt`, `protocol-development`, `protocol-research`, `protocol-bugfix`, `protocol-refactoring`, `protocol-freeze-recovery`, `protocol-session-review` |
| Standards (5) | `standard-agent-quality`, `standard-qa`, `standard-rca`, `standard-tdd`, `standard-cto-review` |
| Learning | `error-learning` |

---

## Типичные сценарии

### Сценарий 1: Новая фича
```
Добавь компонент для отображения статистики по менеджерам
```
→ AI применит: `protocol-development.mdc` + `standard-tdd.mdc`

### Сценарий 2: Исправление бага
```
Не работает фильтрация в таблице сделок, показывает ошибку
```
→ AI применит: `protocol-bugfix.mdc` + `standard-rca.mdc`

### Сценарий 3: Анализ данных
```
Проанализируй структуру parquet файла с контактами
```
→ AI применит: `protocol-research.mdc` + `_base-5wh.mdc`

### Сценарий 4: Рефакторинг
```
Отрефактори функцию calculateDealStats, она слишком длинная
```
→ AI применит: `protocol-refactoring.mdc` + `standard-qa.mdc`

### Сценарий 5: После завершения работы
```
Примени protocol-session-review.mdc для анализа сессии
```
→ AI проанализирует сессию и запишет улучшения

---

## Проверка что инструкции работают

### Признаки что AI следует инструкциям:

1. ✅ Для STANDARD/COMPLEX показывает "Подготовленный промт"
2. ✅ Создаёт TODO list для сложных задач
3. ✅ Пишет уверенность: "Уверенность: 90% → 75%"
4. ✅ Применяет Challenge protocol перед "готово"
5. ✅ Показывает Cross-check результатов
6. ✅ Выводит COMPLETION REPORT в конце

---

## Метрики качества

| Метрика | Target |
|---------|--------|
| Linter errors | 0 |
| Задачи без переделок | >80% |
| Повторные ошибки | <10% |

---

**Версия:** 1.0

