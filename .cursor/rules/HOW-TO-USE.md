# 📖 КАК РАБОТАТЬ С CURSOR RULES

## Краткий ответ

**НЕТ, тебе НЕ нужно писать "используй core-master.mdc".**

Файлы с `alwaysApply: true` применяются **АВТОМАТИЧЕСКИ** к каждому запросу.

---

## Как это работает

### Автоматическое применение

```
Твой запрос 
    ↓
[STANDARD/COMPLEX] → protocol-prepare-prompt.mdc (улучшение промта)
    ↓
core-master.mdc (определяет сложность и тип)
    ↓
protocol-*.mdc (выполняет задачу)
    ↓
standard-*.mdc (верификация)
```

Поскольку `core-master.mdc` имеет `alwaysApply: true`, он **всегда** применяется. Тебе ничего не нужно делать.

### Что происходит внутри

1. Ты пишешь запрос: *"Добавь новый API endpoint для получения данных"*
2. Cursor автоматически загружает `core-master.mdc`
3. AI определяет сложность: 🟡 STANDARD
4. AI улучшает промт (Goal/Context/Constraints/Success)
5. AI видит ключевые слова "добавь" → это **Feature Development**
6. AI применяет `protocol-development.mdc` + `standard-tdd.mdc`
7. AI следует инструкциям из этих файлов
8. В конце проверяет через `standard-agent-quality.mdc`

---

## Структура папки rules/

```
rules/
├── core-master.mdc           # ⭐ Entry point (alwaysApply: true)
├── _base-*.mdc               # 📦 Базовые модули (8 шт)
├── protocol-*.mdc            # 🔄 Протоколы (7 шт)
├── standard-*.mdc            # 📋 Стандарты (5 шт)
├── error-learning.mdc        # 📚 Обучение на ошибках
├── ARCHITECTURE.md           # 📖 Архитектура
└── HOW-TO-USE.md             # 📖 Этот файл
```

### Полный список файлов

| Категория | Файлы |
|-----------|-------|
| Entry Point | `core-master` |
| Base Modules (8) | `_base-confidence`, `_base-todo-usage`, `_base-challenge`, `_base-5wh`, `_base-forbidden`, `_base-crosscheck`, `_base-jtbd-thinking`, `_base-rat` |
| Protocols (7) | `protocol-prepare-prompt`, `protocol-development`, `protocol-research`, `protocol-bugfix`, `protocol-refactoring`, `protocol-freeze-recovery`, `protocol-session-review` |
| Standards (5) | `standard-agent-quality`, `standard-qa`, `standard-rca`, `standard-tdd`, `standard-cto-review` |
| Learning | `error-learning` |

**Подробности:** см. `ARCHITECTURE.md`

---

## Варианты использования

### Вариант 1: Просто пиши запрос (рекомендуется)

```
Добавь фильтрацию по дате в API /api/deals
```

AI сам определит тип задачи, улучшит промт и применит нужный протокол.

### Вариант 2: Явно укажи протокол (если нужен конкретный)

```
Используя protocol-research.mdc, проанализируй структуру файла data.parquet
```

### Вариант 3: Через @ упоминание (Cursor feature)

В Cursor можно использовать `@` для ссылки на правила:
```
@protocol-bugfix Исправь ошибку в компоненте DataTable
```

### Вариант 4: Одиночные инструкции

```
@rules_alone/backlog-to-rules Внедри улучшения из backlog
```

---

## Когда НУЖНО явно указывать протокол

| Ситуация | Что делать |
|----------|------------|
| Обычная задача | Ничего, AI сам выберет |
| AI выбрал неправильный протокол | Укажи явно: "Используй protocol-research.mdc" |
| Хочешь принудительно применить | Упомяни файл: "@protocol-refactoring" |
| После сессии для review | "Примени protocol-session-review.mdc" |
| Нужна одиночная проверка | "@rules_alone/core-duplicate-check" |
| Внедрить улучшения | "@rules_alone/backlog-to-rules" |

---

## Проверка что инструкции работают

### Признаки что AI следует инструкциям:

1. ✅ Для STANDARD/COMPLEX показывает "Подготовленный промт" (Goal/Context/Constraints/Success)
2. ✅ Создаёт TODO list для сложных задач
3. ✅ Пишет уверенность: "Уверенность: 90% → 75% — не проверил на данных"
4. ✅ Делает Duplicate Check перед созданием файлов
5. ✅ Применяет Challenge protocol перед "готово"
6. ✅ Показывает Cross-check результатов
7. ✅ Проверяет метрики из `standard-agent-quality.mdc`

### Если AI не следует инструкциям:

1. Проверь что `core-master.mdc` имеет `alwaysApply: true`
2. Попробуй явно упомянуть: "Следуй core-master.mdc"
3. Перезапусти Cursor (иногда кэш)

---

## Типичные сценарии

### Сценарий 1: Новая фича
```
Добавь компонент для отображения статистики
```
→ AI улучшит промт + применит: `protocol-development.mdc` + `standard-tdd.mdc`

### Сценарий 2: Исправление бага
```
Не работает фильтрация в таблице, показывает ошибку
```
→ AI применит: `protocol-bugfix.mdc` + `standard-rca.mdc`

### Сценарий 3: Анализ данных
```
Проанализируй структуру parquet файла с данными
```
→ AI применит: `protocol-research.mdc` + `_base-5wh.mdc`

### Сценарий 4: Рефакторинг
```
Отрефактори функцию calculateStats, она слишком длинная
```
→ AI применит: `protocol-refactoring.mdc` + `standard-qa.mdc`

### Сценарий 5: После завершения работы
```
Примени protocol-session-review.mdc для анализа сессии
```
→ AI проанализирует сессию и запишет улучшения

### Сценарий 6: Проверка на дубликаты
```
@rules_alone/core-duplicate-check Перед созданием UserCard проверь дубликаты
```
→ AI применит одиночную инструкцию

### Сценарий 7: Внедрение улучшений
```
@rules_alone/backlog-to-rules Внедри улучшения из improvements-backlog.md
```
→ AI проанализирует backlog и внесёт изменения в инструкции

---

## Определение сложности

| Сложность | Признаки | Что делает AI |
|-----------|----------|---------------|
| 🟢 SIMPLE | 1-2 файла, очевидный результат | Пропускает подготовку промта → EXECUTE → VERIFY |
| 🟡 STANDARD | Новая функциональность, несколько файлов | Quick Prompt Prep → Полный 4-фазный протокол |
| 🔴 COMPLEX | Архитектура, критические данные | Полный prepare-prompt → Протокол + CTO Review |

---

## FAQ

### Q: Мне нужно читать все инструкции?
**A:** Нет. Просто работай как обычно. Инструкции для AI, не для тебя.

### Q: Что если AI игнорирует инструкции?
**A:** Явно напомни: "Следуй инструкциям из core-master.mdc"

### Q: Можно ли отключить инструкции?
**A:** Поменяй `alwaysApply: true` на `false` в `core-master.mdc`

### Q: Как добавить свои правила?
**A:** Создай файл по образцу существующего протокола/стандарта, положи в `rules/`, добавь в `core-master.mdc`

### Q: Где одиночные инструкции?
**A:** В `.cursor/rules_alone/` — 4 файла, вызываются явно через `@`

### Q: Как улучшить качество работы AI?
**A:** Используй `@rules_alone/backlog-to-rules` для внедрения улучшений из `{projectname}-improvements-backlog.md`

---

## Метрики качества

| Метрика | Target | Где проверить |
|---------|--------|---------------|
| Linter errors | 0 | `read_lints` |
| Задачи без переделок | >80% | Session Review |
| Повторные ошибки | <10% | Error Log |

---

## Быстрый старт

1. **Пиши запросы как обычно** — инструкции применятся автоматически
2. **AI улучшит твой промт** — для STANDARD/COMPLEX задач покажет Goal/Context/Constraints/Success
3. **В конце сессии** — попроси "Примени protocol-session-review.mdc"
4. **Периодически** — вызови "@rules_alone/backlog-to-rules" для внедрения улучшений

---

## Связанные файлы

| Файл | Где | Назначение |
|------|-----|------------|
| `AGENTS.md` | корень проекта | Quick Start для AI (опционально) |
| `ARCHITECTURE.md` | `.cursor/rules/` | Структура и flow |
| `CHANGELOG.md` | `.cursor/` | История изменений |
| `improvements-backlog.md` | `.cursor_additional/{projectname}/` | Накопление улучшений |

---

**Версия:** 4.0
**Дата:** 2026-01-10

