---
description:
globs:
alwaysApply: true
tags: [web, auth, basic]
---

# 🌐 Site access: Basic Auth (always)

## Context

- Применять при любых проверках сайта/страниц/эндпоинтов, когда сервер отвечает **401 Unauthorized**
- В некоторых окружениях включена **Basic access authentication**

## Requirements

- Всегда учитывать, что без Basic Auth можно получить 401 даже на публичной странице
- Всегда брать креды из `.cursor/.secrets/site-basic-auth.json`
- Никогда не вставлять логин/пароль в ответы пользователю, в логи или в code snippets
- При проверках через curl использовать Basic Auth (`-u`)
- При проверках через браузер/получение HTML — вводить Basic Auth креды из секретов, если всплывает диалог авторизации

## Examples

<example>
Проверка страницы через curl с Basic Auth:

curl -u "<user>:<pass>" "<url>"
</example>

<example type="invalid">
Неверно: вставлять пароль в rule-файл или в сообщение пользователю:

curl -u "<user>:<pass>" "<url>"  # ❌ если <user>/<pass> взяты из текста сообщения/правила
</example>

## Critical Points

<critical>
  - ALWAYS: если видишь 401 при проверке сайта — сначала добавь Basic Auth
  - NEVER: не публикуй креды (в правилах/ответах/логах)
  - ENSURE: секреты лежат в `.cursor/.secrets/` и этот путь заигнорен
</critical>

