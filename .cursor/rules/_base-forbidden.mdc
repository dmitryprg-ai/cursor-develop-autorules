---
description: Атомарный модуль запрещённых действий AI агента
alwaysApply: false
---
# 🚫 ЗАПРЕЩЁННЫЕ ДЕЙСТВИЯ

## Критические запреты (NEVER)

### Файлы и структура

| ❌ ЗАПРЕЩЕНО | ✅ ПРАВИЛЬНО |
|--------------|-------------|
| Создавать файлы с `_fixed`, `_final`, `_FINAL`, `_new`, `_v2` | Редактировать исходный файл |
| Перезаписывать файлы целиком | Делать точечные изменения |
| Создавать дубликаты скриптов | Найти и использовать существующий |
| Удалять файлы если уверенность < 100% | Спросить пользователя |
| Создавать файлы если уверенность < 20% | Уточнить требования |

> **WHY постфиксы `_fixed`, `_final`:**  
> Реальный случай: AI создал `Component_fixed.tsx` вместо редактирования оригинала.
> Пользователь не знал какой файл актуальный → 20 минут на разбор дублей.
> **Правило:** Всегда редактируй исходный файл напрямую.

### Код и архитектура

| ❌ ЗАПРЕЩЕНО | ✅ ПРАВИЛЬНО |
|--------------|-------------|
| Использовать `try: ... except: pass` | Обрабатывать ошибки явно |
| Использовать `sys.path.append()` | Правильные imports |
| Писать "большие монолитные диффы" (300+ строк) | Маленькие атомарные изменения |
| Дублировать код | Использовать `_base-*` модули |
| Использовать `alwaysApply: true` (кроме core-master.mdc) | `alwaysApply: false` |

### Данные и предположения

| ❌ ЗАПРЕЩЕНО | ✅ ПРАВИЛЬНО |
|--------------|-------------|
| Делать выводы "по памяти модели" | Проверить в данных: dtypes, head(), nunique() |
| Предполагать структуру без проверки | Сначала показать schema |
| Ставить лимиты по умолчанию | Явно указывать или анализировать всё |
| Игнорировать ошибки и exceptions | Анализировать каждую ошибку |
| Фильтровать данные "под ожидаемый результат" | Анализировать сырые данные |

> **WHY проверять типы данных:**  
> Реальный случай: `managerId` из API приходил как строка `'26698'`, а в коде сравнивался с числом `26698`.
> `Map.get(26698)` не находил ключ `'26698'` → баг "0 записей".
> **Правило:** При связывании данных из разных источников ВСЕГДА проверяй типы!

### Заявления и отчёты

| ❌ ЗАПРЕЩЕНО | ✅ ПРАВИЛЬНО |
|--------------|-------------|
| "Успешно завершено" без проверки | Показать evidence |
| "Все тесты прошли" без запуска | Показать output тестов |
| "Найдено X записей" без просмотра | Открыть и показать sample |
| "Готово" без cross-check | Выполнить независимую проверку |
| "По идее должно работать" | Запустить и показать результат |

> **WHY не говорить "готово" без проверки:**  
> Реальный случай: AI заявил "файл создан успешно", но при открытии — файл пустой (ошибка записи).
> Пользователь потерял 15 минут на диагностику.
> **Правило:** Открой и покажи содержимое перед "готово".

### Процесс работы

| ❌ ЗАПРЕЩЕНО | ✅ ПРАВИЛЬНО |
|--------------|-------------|
| Продолжать после ошибки в терминале | STOP → RCA → Fix → Verify |
| Игнорировать warnings | Проанализировать и исправить |
| "Срезать углы" | Решать корневую проблему |
| Использовать workarounds | Найти правильное решение |
| Одновременно несколько `in_progress` TODO | Только 1 active task |

## Запрещённые формулировки

```
❌ "Успешно извлечено" без проверки содержимого
❌ "Найдено X записей" без открытия файлов  
❌ "Все работает" без тестирования
❌ "Завершено" без валидации
❌ "Готово" без проверки качества
❌ "Судя по тестам/докам..." (без реальной проверки)
❌ "По идее должно работать" (без тестирования)
❌ "Галочка проставлена" (без проверки результата)
```

## Что делать при нарушении

1. **Заметил нарушение → STOP**
2. **Применить `standard-rca.mdc`** — найти root cause
3. **Записать в `error-learning.mdc`** — для обучения
4. **Исправить корректно** — не workaround

---
**Использование:** Вызывается из `core-master.mdc` и всех протоколов
